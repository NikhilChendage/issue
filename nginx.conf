worker_processes 1;

events { worker_connections 1024; }

http {
    include mime.types;
    sendfile on;

    server {
        listen 80;
        server_name _;

        root /var/www/html;
        index index.php index.html;
worker_processes 1;

events { 
    worker_connections 1024; 
}

http {
    include mime.types;
    sendfile on;

    # ⭐ Increase upload limit (fix 413 error)
    client_max_body_size 200M;

    server {
        listen 80;
        server_name _;

        root /var/www/html;
        index index.php index.html;

        # ⭐ You can also put this here (optional)
        client_max_body_size 200M;

        location / {
            try_files $uri /index.php?$query_string;
        }

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            include fastcgi.conf;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }
    }
}

        location / {
            try_files $uri /index.php?$query_string;
        }

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            include fastcgi.conf;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }
    }
}
